<!DOCTYPE html>
<html lang="en">
  <head>
    <meta charset="utf-8">
    <title>My Project</title>

    <%= csrf_meta_tags %>

    <!-- styles -->
    <link type="text/css" href="/assets/jquery-ui-1.8.18.custom/css/redmond/jquery-ui-1.8.18.custom.css" rel="Stylesheet" />
    <%= stylesheet_link_tag "application", :media => "all" %>

    <!-- HTML5 shim, for IE6-8 support of HTML elements -->
    <!--[if lt IE 9]>
      <script src="http://html5shim.googlecode.com/svn/trunk/html5.js" type="text/javascript"></script>
    <![endif]-->

    <!-- fav and touch icons -->
    <link href="images/favicon.ico" rel="shortcut icon">
    <link href="images/apple-touch-icon.png" rel="apple-touch-icon">
    <link href="images/apple-touch-icon-72x72.png" rel="apple-touch-icon" sizes="72x72">
    <link href="images/apple-touch-icon-114x114.png" rel="apple-touch-icon" sizes="114x114">
  </head>
  <body>

    <div class="navbar navbar-fixed-top">
      <div class="navbar-inner">
          <div class="container">
          <a class="btn btn-navbar" data-target=".nav-collapse" data-toggle="collapse">
            <span class="i-bar"></span>
            <span class="i-bar"></span>
            <span class="i-bar"></span>
          </a>
          <%= link_to "My Project", root_url, :class => 'brand i-brand' %>
          <div class="container nav-collapse">
            <ul class="nav i-navbar-nav">
                <li><%= link_to "אודותינו", "home"  %></li>
                <li><%= link_to "צור קשר", "home"  %></li>
                <li><%= link_to "החשבון שלי", "home"  %></li>
            </ul>
          </div><!--/.nav-collapse -->
        </div>
      </div>
    </div>
    <div class="container">
        <div class="content">
              <% [:notice, :error, :alert].each do |level| %>
                <% unless flash[level].blank? %>
                  <div data-alert="alert" class="alert alert-<%= flash_class(level) %> fade in">
                    <a class="close" data-dismiss="alert" href="#">&times;</a>
                    <%= content_tag :p, flash[level] %>
                  </div>
                <% end %>
              <% end %>
              <div class="mainContainer">
                <%= yield %>
              </div>
        </div>
    </div> <!-- /container -->

    <hr>
    <footer>
        <p>&copy; My Project 2012</p>
    </footer>
    
    <!-- javascript
    ================================================== -->
    <!-- Placed at the end of the document so the pages load faster -->
    <%= javascript_include_tag "application" %>
  </body>
</html>
